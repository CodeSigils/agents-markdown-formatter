# Project-level npm configuration
# Auth tokens belong in ~/.npmrc, not here

# Pin exact dependency versions (no ^ ~ prefixes)
save-exact=true

# Script hooks
# prepublishOnly and postversion are in package.json
